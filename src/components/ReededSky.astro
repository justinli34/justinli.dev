<fieldset
  class="sky-window"
  aria-label="Reeded glass sky. Click a slice to toggle day and night, or hold and drag to paint multiple slices. Match all slices to change the site theme."
  hidden
>
  <canvas class="sky-canvas" aria-hidden="true"></canvas>
  <div class="sky-slices"></div>
  <div class="glass-edge" aria-hidden="true"></div>
</fieldset>

<script>
  import { initReededSky } from "@/scripts/reeded-sky";

  initReededSky();
</script>

<style>
  .sky-window {
    position: relative;
    min-width: 0;
    margin: 0;
    padding: 0;
    height: clamp(260px, 28vw, 340px);
    overflow: hidden;
    isolation: isolate;
    background: var(--color-sky-bg);
    border: 1px solid var(--color-sky-border);
    border-radius: 3px;
  }

  .sky-canvas,
  .sky-slices,
  .glass-edge {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
  }

  .sky-canvas {
    display: block;
  }

  .sky-slices {
    display: flex;
    touch-action: none;
    user-select: none;
  }

  .sky-slices :global(button) {
    flex: 1;
    min-width: 0;
    padding: 0;
    border: 0;
    border-radius: 0;
    background: transparent;
    cursor: pointer;
  }

  .sky-slices :global(button:focus-visible) {
    outline: 2px solid #fff;
    outline-offset: -3px;
    box-shadow: inset 0 0 0 4px #142d43;
  }

  .glass-edge {
    pointer-events: none;
    border-radius: inherit;
    box-shadow:
      inset 0 1px 1px #f0ffffb0,
      inset 0 -2px 3px #17364170,
      inset 2px 0 3px #e6ffff70,
      inset -2px 0 3px #17364160;
  }

  @media (max-width: 700px) {
    .sky-window {
      height: 280px;
    }
  }
</style>
