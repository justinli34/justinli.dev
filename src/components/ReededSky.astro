<div
  class="sky-window"
  role="img"
  aria-label="Clouds and light refracted through vertical, reeded glass."
  hidden
>
  <canvas class="sky-canvas" aria-hidden="true"></canvas>
  <div class="glass-edge" aria-hidden="true"></div>
</div>

<script>
  import { initReededSky } from "@/scripts/reeded-sky";

  initReededSky();
</script>

<style>
  .sky-window {
    position: relative;
    height: clamp(260px, 28vw, 340px);
    overflow: hidden;
    isolation: isolate;
    background: #70abb9;
    border: 1px solid #709b9f;
    border-radius: 3px;
  }

  .sky-canvas,
  .glass-edge {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
  }

  :global([data-theme="dark"]) .sky-window {
    border-color: #35474e;
    background: #142d43;
  }

  .sky-canvas {
    display: block;
  }

  .glass-edge {
    pointer-events: none;
    border-radius: inherit;
    box-shadow:
      inset 0 1px 1px #f0ffffb0,
      inset 0 -2px 3px #17364170,
      inset 2px 0 3px #e6ffff70,
      inset -2px 0 3px #17364160;
  }

  @media (max-width: 700px) {
    .sky-window {
      height: 280px;
    }
  }
</style>
